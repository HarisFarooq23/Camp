# Local development — API is proxied to localhost:5000
VITE_API_URL=

# Production (Vercel) — set to your hosted API URL, e.g. https://campus-api.railway.app
# VITE_API_URL=https://your-api-host.example.com

# Optional Instagram scraper login (used when running scraper.py directly or via API)
# IG_PASSWORD=your_throwaway_password
